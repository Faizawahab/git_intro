git clone. downloading the project
git fetch. getting the history
git pull. getting the current changes
git push



git init  (to set git eyes on it)
git add index.txt
git add. (selects everything)
git commit -m 
git remote add origion https://github.com/Faizawahab/git_intro
gut push origion 
--allow-unrelated-histories
rm -rf .git (to delete a git repo on your local machine)
--force (override the main branch online)
